{
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq nginx curl 2>/dev/null; chmod +x scripts/*.sh",
  "start": "cd /workspace && ./scripts/cloud-dev-up.sh start",
  "terminals": [
    {
      "name": "datagent-landing",
      "command": "cd /workspace && ./scripts/cloud-dev-up.sh foreground"
    }
  ]
}
